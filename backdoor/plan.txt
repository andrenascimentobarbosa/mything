Steps to create the malware.

1. Connection: [socket] Client (target) establishes a connection with the server.
The server will send and receive data such as commands, outputs
and screenshots.

2. Commands: [subprocess] via a while loop and a input, the server will encoded 
and send the command to the client waiting for an output to be decoed and read it; 
on the client side, the commando will be received, decoded and excuted sending the 
encoded output to the server.

3. Screenshot: [pyscreenshot, paramiko] create a function that takes a screenshot 
using the pyscreenshot, save it, send it to the server via ssh using paramiko and 
delete it from the client machinne.

 
